<%= @post.title %>
<%= @post.body %>
<hr>
<div>
  <%= link_to "Edit", edit_post_path(@post), :class=>"btn btn-default" %>
      
       <%= link_to "Delete", post_path(@post), method: :delete, data: {confirm: "Are you sure?"}, :class=>"btn btn-danger" %>
      </div>
<hr>


<h3>
Add a new comment
</h3>

<%= render "comments/comments" %>
<%= render "comments/form" %>form